const React = require('react');

module.exports = (props) => {
  return (
    <div>
      <h1>Home/splash page</h1>

      <form action="." method="POST">
        <button type="submit">Create a new document</button>
      </form>
    </div>
  );
}
